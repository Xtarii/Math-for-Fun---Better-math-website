import { ReactElement } from "react";

/**
 * Authenticator Element
 *
 * Supabase react authentication
 * element. Holds functions to
 * login and create accounts.
 */
export default function Authenticator() : ReactElement {
    return(<div>
    </div>);
}
